# OS
.DS_Store

# Unrelated local downloads (VSCode/OBS/Python installers), not project source
Installs/

# Node / Electron
node_modules/
desktop/dist/
desktop/out/
desktop/release/

# Python
__pycache__/
*.pyc
.venv/
venv/
engine/build/
engine/dist/
engine/*.spec.bak

# Local runtime state (the contribution queue itself now lives outside the
# repo, in Electron's userData directory - see desktop/src/main/paths.js)
*.local.json

# Secrets - see desktop/.env.example
.env
